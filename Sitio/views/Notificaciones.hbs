<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notificaciones Alumno</title>
    <link rel="stylesheet" href="../css/formulario.css"> <!-- Verifica que la ruta sea correcta -->
</head>
<body>
<div class="formulario-container">
    <h1>SIEMBRA VALORES</h1>
    <h2>Notificaciones Alumno</h2>

    <form>
        <div class="form-row">
            <label for="fecha-reporte">Fecha del Reporte:</label>
            <input type="date" id="fecha-reporte" name="fecha-reporte" class="input-text">
        </div>
        {{#each resultado}}
        <div class="form-row">
            <label>ID del alumno:</label>
            <span class="label-text" id="id-arbol">{{ID_US}}</span> <!-- Etiqueta con ID del árbol -->
        </div>

        <div class="form-row">
            <label>Nombre:</label>
            <span class="label-text" id="nombre-arbol">{{NOMBRE}} </span> <!-- Etiqueta con nombre del árbol -->
        </div>
                {{/each}}
        <input id="id" type="hidden" name="id" value="{{id}}">

        <div class="form-row">
            <label for="mensaje-alumno">Mensaje para el alumno:</label>
            <textarea id="mensaje-alumno" name="mensaje-alumno" rows="5" maxlength="255" class="input-text"></textarea> <!-- TextBox grande para el mensaje -->
        </div>

        <button type="button" class="btn-enviar">Enviar Notificación</button>
    </form>
</div>
</body>
</html>
